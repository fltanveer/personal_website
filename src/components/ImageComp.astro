---
import { Image } from "@astrojs/image/components";
export interface Props {
  src: string;
  alt: string;
}

const { src, alt } = Astro.props;
---

<Image src={src} alt={alt} width={320} aspectRatio="2:1" />
